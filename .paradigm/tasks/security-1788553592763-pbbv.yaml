id: security-1788553592763-pbbv
agent: security
model: opus
status: pending
created: '2026-09-04T20:26:32.763Z'
task: >-
  Review security aspects of: Review this diff (do NOT edit files) against
  .paradigm/specs/wallet-checkout.md: the #wallet-service implementation in
  server/src/services/wallet.ts wiring @circle-fin/developer-controlled-wallets
  (one EOA wallet per user on ARC-TESTNET, wallet set + user wallet ids
  persisted in a lazily-created Postgres table, USDC token address resolved
  dynamically via getWalletTokenBalance with a documented fallback constant,
  createTransaction called with an idempotencyKey). Diff is at
  /tmp/wallet-diff.patch. Reviewer: check idempotency handling, error handling
  on missing wallet, whether persisting Circle wallet ids in a plain
  user_wallets table is an acceptable custody boundary given the entity secret
  stays server-side, and whether the fallback USDC token address is a risk.
  Security: check for any secret leakage, replay risk, and whether
  unauthenticated callers could trigger getOrCreateUserWallet/transferUsdc
  (note: ^authenticated/^checkout-authorized gates are not wired into the route
  yet, still TODO).
context:
  systemPrompt: >
    # Security Agent Context


    > Project: agentic-commerce

    > Discipline: backend


    ## Project Overview


    This project uses Paradigm for structured AI-assisted development.

    All context, symbols, and specifications live in the .paradigm/ directory.



    ## Symbol System


    | Symbol | Meaning | Description |

    |--------|---------|-------------|

    | `#` | Component | Any documented code unit (feature, service, module,
    integration) |

    | `$` | Flow | Multi-step processes or user journeys |

    | `^` | Gate | Access control points and authorization rules |

    | `!` | Signal | Events emitted for side effects |

    | `~` | Aspect | Cross-cutting rules with required code anchors |


    ## Conventions


    - Add descriptions to all components and gates

    - Keep gates minimal - one responsibility per gate


    ## Symbols in Scope


    - `#wallet-service` (component)

    - `^authenticated` (gate)

    - `^checkout-authorized` (gate)


    ## Tips for This Role


    - Focus on ^gate implementations

    - Check for OWASP top 10 vulnerabilities

    - Flag issues but do NOT implement fixes

    - Review auth flows and session handling
  symbols:
    - '#wallet-service'
    - ^authenticated
    - ^checkout-authorized
role: >-
  You audit for security issues, especially around ^gates.

  Review auth flows, check for vulnerabilities.

  Flag issues but do NOT implement fixes - hand to Builder for that.

  Repo rule: every prompt used for this work must be logged via `npm run
  log-prompt -- "<prompt>"` (writes to prompts.jsonl). See CLAUDE.md.
focus:
  reads:
    - ^gates
    - '!signals'
    - portal.yaml
    - src/**
  writes:
    - security/*.md
